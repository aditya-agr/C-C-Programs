#include<stdio.h>
#include<conio.h>
int main()
{
    int a;
    printf("Enter the ASCII number :");
    scanf("%d", &a);

    printf("The character of ASCII value is %c.", a);
    getch(); 
    return 0;
}